//
// Created by zwpdbh on 25/11/2017.
//

#include "lib.h"

int lib::endPre(const std::string &strExp, unsigned long first) {
    unsigned long last = strExp.length() - 1;

    if (first < 0 || first > last) {
        return -1;
    }

    char ch = strExp.at(first);
//    if

    return 0;

}